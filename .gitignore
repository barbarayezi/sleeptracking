sleep_tracker.db
sleep_tracker.db-shm
sleep_tracker.db-wal
__pycache__/
*.pyc
.env
venv/
.logs/
.active_port
.last_started
.playwright-mcp/

# Third-party sub-project: independent Node app with its own node_modules,
# build cache (*.tsbuildinfo) and .env — not part of this repo's source
health-agent/

# Tool / IDE generated state (machine-specific, not source code)
.qoder/
.workbuddy/
*.log
sleeptracker_launchd.log
# design drafts (not part of the app)
design-drafts/

# UI redesign screenshots & one-off deploy scripts (contain plaintext secrets — never commit)
/debug_error.png
/redesign_*.png
/scripts/render_deploy.py
